[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "roadyaml"
version = "0.1.0"
description = "BlackRoad OS Component"
requires-python = ">=3.10"
